import baseWorker from "./worker-live-news.js";

const PRIDE_LEAGUE_ID="astbqxhwmk4b6bg9";
const FANTRAX_GENERAL="https://www.fantrax.com/fxea/general/";
const TEAM_IDS={ATL:1,BUF:2,CHI:3,CIN:4,CLE:5,DAL:6,DEN:7,DET:8,GB:9,TEN:10,IND:11,KC:12,LV:13,LAR:14,MIA:15,MIN:16,NE:17,NO:18,NYG:19,NYJ:20,PHI:21,ARI:22,PIT:23,LAC:24,SF:25,SEA:26,TB:27,WAS:28,WSH:28,CAR:29,JAX:30,BAL:33,HOU:34};
const YEAR=()=>new Date().getUTCFullYear();
const DEPTH_URL=id=>`https://sports.core.api.espn.com/v2/sports/football/leagues/nfl/seasons/${YEAR()}/teams/${id}/depthcharts`;
const ROSTER_URL=id=>`https://site.api.espn.com/apis/site/v2/sports/football/nfl/teams/${id}/roster`;
function cors(){return{"Access-Control-Allow-Origin":"https://gmslocker.com","Access-Control-Allow-Methods":"GET, POST, DELETE, OPTIONS","Access-Control-Allow-Headers":"Accept, Content-Type, Authorization","Cache-Control":"no-store",Vary:"Origin"};}
function json(body,status=200){return new Response(JSON.stringify(body),{status,headers:{"Content-Type":"application/json; charset=utf-8",...cors()}});}
function clean(value){return String(value||"").replace(/\s+/g," ").trim();}
function teamCode(value){return clean(value).toUpperCase().replace(/[^A-Z]/g,"").replace(/^WSH$/,"WAS").slice(0,4);}
function athleteId(value){const text=String(value||"");return text.match(/\/athletes\/(\d+)/)?.[1]||text.match(/^(\d+)$/)?.[1]||null;}
function athleteName(row){return clean(row?.athlete?.displayName||row?.athlete?.fullName||row?.displayName||row?.fullName||row?.name||row?.player?.displayName||row?.player?.fullName);}
function slotBase(key,position){const raw=clean(key||position?.abbreviation||position?.name).toUpperCase().replace(/[^A-Z0-9]/g,"");const aliases={LEFTWIDERECEIVER:"LWR",RIGHTWIDERECEIVER:"RWR",SLOTWIDERECEIVER:"SWR",SLOTRECEIVER:"SWR",LEFTCORNERBACK:"LCB",RIGHTCORNERBACK:"RCB",NICKELCORNERBACK:"NCB",NICKELBACK:"NCB",FREESAFETY:"FS",STRONGSAFETY:"SS",MIDDLELINEBACKER:"MIKE",WEAKSIDELINEBACKER:"WILL",STRONGSIDELINEBACKER:"SAM",LEFTDEFENSIVEEND:"LDE",RIGHTDEFENSIVEEND:"RDE",NOSETACKLE:"NT"};if(aliases[raw])return aliases[raw];if(/^(LWR|RWR|SWR|XWR|ZWR|LCB|RCB|NCB|SCB|FS|SS|MIKE|WILL|SAM|LOLB|ROLB|LDE|RDE|NT|DT|RB|QB|TE|FB|WR|CB|LB|DE|KR|PR|LS|P|K)$/.test(raw))return raw;return raw.slice(0,10)||"DEPTH";}
function collectRosterNames(node,map){if(!node)return;if(Array.isArray(node)){node.forEach(item=>collectRosterNames(item,map));return;}if(typeof node!=="object")return;const id=String(node.id||node.athlete?.id||"");const name=athleteName(node)||athleteName(node.athlete);if(id&&name)map.set(id,name);Object.values(node).forEach(value=>collectRosterNames(value,map));}
function parseDepth(depth,team,nameMap){const rows=[],seen=new Set();for(const chart of depth?.items||[]){const formation=clean(chart?.name)||null;for(const [key,group] of Object.entries(chart?.positions||{})){const base=slotBase(key,group?.position),generic=/^(WR|CB|LB|DB|DL|DE|DT)$/.test(base);const athletes=Array.isArray(group?.athletes)?group.athletes:Array.isArray(group?.players)?group.players:[];athletes.forEach((row,index)=>{const ref=row?.athlete?.$ref||row?.$ref||row?.athlete?.ref||"";const id=String(row?.athlete?.id||row?.id||athleteId(ref)||"");const name=athleteName(row)||athleteName(row?.athlete)||nameMap.get(id)||"";if(!name)return;const rank=Number(row?.rank??row?.depth??row?.order)||index+1;const slot=`${base}${rank}`;const unique=`${team}|${name.toLowerCase()}|${slot}`;if(seen.has(unique))return;seen.add(unique);rows.push({name,athleteId:id||null,team,slot,baseSlot:base,depth:rank,providerPosition:clean(group?.position?.name||group?.position?.displayName||key)||null,providerGeneric:generic,formation});});}}return rows;}
async function fetchJson(url){const response=await fetch(url,{headers:{Accept:"application/json","User-Agent":"GMSLocker/2.8"},cf:{cacheTtl:60,cacheEverything:true}});if(!response.ok)throw new Error(`HTTP ${response.status}`);return response.json();}
async function oneTeam(team){const id=TEAM_IDS[team];if(!id)return{team,rows:[],error:"Unknown ESPN team mapping"};try{const [depth,roster]=await Promise.all([fetchJson(DEPTH_URL(id)),fetchJson(ROSTER_URL(id))]);const names=new Map();collectRosterNames(roster,names);return{team,rows:parseDepth(depth,team,names),error:null};}catch(error){return{team,rows:[],error:String(error?.message||error)};}}
async function depthCharts(url){const teams=[...new Set(String(url.searchParams.get("teams")||"").split(",").map(teamCode).filter(Boolean))].slice(0,32);if(!teams.length)return json({teams:{},players:[],source:"ESPN NFL depth charts",syncedAt:new Date().toISOString(),warning:"No NFL teams requested"});const results=await Promise.all(teams.map(oneTeam)),byTeam={},players=[],errors=[];for(const result of results){byTeam[result.team]=result.rows;players.push(...result.rows);if(result.error)errors.push(`${result.team}: ${result.error}`);}return json({teams:byTeam,players,source:"ESPN NFL depth charts",syncedAt:new Date().toISOString(),season:YEAR(),requestedTeams:teams.length,returnedTeams:Object.keys(byTeam).length,errors});}

async function fantraxPublic(endpoint){const u=new URL(FANTRAX_GENERAL+endpoint);u.searchParams.set("leagueId",PRIDE_LEAGUE_ID);const response=await fetch(u.toString(),{headers:{Accept:"application/json","User-Agent":"GMSLocker/2.8"},cf:{cacheTtl:30,cacheEverything:true}});if(!response.ok)throw new Error(`${endpoint} HTTP ${response.status}`);return response.json();}
function normalizeRosterTeam(id,row){const items=Array.isArray(row?.rosterItems)?row.rosterItems:[];return{id,name:row?.teamName||id,players:items.map(item=>{const p=item?.player||item;return{id:String(p?.id||item?.id||""),name:p?.name||p?.playerName||item?.name||"Unknown",position:p?.position||p?.pos||item?.position||"",nflTeam:p?.team||p?.nflTeam||item?.team||"",salary:item?.salary??p?.salary??null,contract:item?.contract??item?.contractInfo??null,rosterSlot:item?.rosterSlot||item?.status||""};})};}
async function publicPrideLeague(){try{const [rosters,info,picks]=await Promise.all([fantraxPublic("getTeamRosters"),fantraxPublic("getLeagueInfo").catch(()=>null),fantraxPublic("getDraftPicks").catch(()=>null)]);const teams=Object.entries(rosters?.rosters||{}).map(([id,row])=>normalizeRosterTeam(id,row));return json({ok:true,leagueId:PRIDE_LEAGUE_ID,leagueName:info?.leagueName||info?.name||"Pride Dynasty",teams,picks:picks?.futureDraftPicks||picks||null,syncedAt:new Date().toISOString(),source:"Fantrax live read-only"});}catch(error){return json({ok:false,error:"Fantrax public league sync failed",detail:String(error?.message||error),leagueId:PRIDE_LEAGUE_ID},502);}}

function hex(buf){return[...new Uint8Array(buf)].map(b=>b.toString(16).padStart(2,"0")).join("");}
async function sha(value){return hex(await crypto.subtle.digest("SHA-256",new TextEncoder().encode(String(value))));}
async function passwordHash(password,saltHex){const salt=Uint8Array.from((saltHex.match(/.{2}/g)||[]).map(x=>parseInt(x,16)));const key=await crypto.subtle.importKey("raw",new TextEncoder().encode(password),"PBKDF2",false,["deriveBits"]);return hex(await crypto.subtle.deriveBits({name:"PBKDF2",hash:"SHA-256",salt,iterations:100000},key,256));}
async function legacyPasswordHash(password,saltHex){const salt=Uint8Array.from((saltHex.match(/.{2}/g)||[]).map(x=>parseInt(x,16)));const encoder=new TextEncoder();const firstMaterial=await crypto.subtle.importKey("raw",encoder.encode(password),"PBKDF2",false,["deriveBits"]);const firstPass=await crypto.subtle.deriveBits({name:"PBKDF2",hash:"SHA-256",salt,iterations:100000},firstMaterial,256);const secondMaterial=await crypto.subtle.importKey("raw",firstPass,"PBKDF2",false,["deriveBits"]);const secondSalt=await crypto.subtle.digest("SHA-256",new Uint8Array([...salt,...encoder.encode("gmslocker-password-v2")]));return hex(await crypto.subtle.deriveBits({name:"PBKDF2",hash:"SHA-256",salt:secondSalt,iterations:100000},secondMaterial,256));}
function randomToken(){const a=new Uint8Array(32);crypto.getRandomValues(a);return btoa(String.fromCharCode(...a)).replace(/\+/g,"-").replace(/\//g,"_").replace(/=+$/,"");}
async function flexibleLogin(request,env){if(request.method!=="POST")return json({error:"POST only"},405);if(!env.DB)return json({error:"Authentication database unavailable"},503);const body=await request.json().catch(()=>({}));const identifier=String(body.username||body.email||"").trim().toLowerCase();const password=String(body.password||"");if(!identifier||!password)return json({error:"Enter your username/email and password"},400);const user=await env.DB.prepare("SELECT id,username,email,password_hash,password_salt FROM users WHERE lower(username)=? OR lower(email)=? LIMIT 1").bind(identifier,identifier).first();if(!user||!user.password_hash||!user.password_salt)return json({error:"Username/email or password incorrect"},401);const current=await passwordHash(password,user.password_salt);let valid=current===user.password_hash;if(!valid)valid=(await legacyPasswordHash(password,user.password_salt))===user.password_hash;if(!valid)return json({error:"Username/email or password incorrect"},401);const token=randomToken();await env.DB.prepare("INSERT INTO user_sessions (token_hash,user_id,expires_at) VALUES (?,?,datetime('now','+30 days'))").bind(await sha(token),user.id).run();await env.DB.prepare("UPDATE users SET last_login_at=CURRENT_TIMESTAMP WHERE id=?").bind(user.id).run();return json({token,user:{id:user.id,username:user.username,email:user.email}});}

export default{async fetch(request,env,ctx){const url=new URL(request.url);if(request.method==="GET"&&url.pathname==="/depth-charts")return depthCharts(url);if(request.method==="GET"&&url.pathname==="/public/pride-league")return publicPrideLeague();if(url.pathname==="/auth/login")return flexibleLogin(request,env);return baseWorker.fetch(request,env,ctx);}};
