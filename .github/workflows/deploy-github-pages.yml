name: Deploy GMS Locker to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - "*.html"
      - "public/**"
      - "manifest.webmanifest"
      - "service-worker.js"
      - "sponsor-page.css"
      - "*.jpeg"
      - "*.svg"
      - "*.png"
      - "CNAME"
      - ".github/workflows/deploy-github-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Stage frontend-only site
        run: |
          mkdir -p _site/public
          cp *.html manifest.webmanifest service-worker.js sponsor-page.css CNAME _site/
          cp *.jpeg *.svg *.png _site/
          cp public/*.js public/*.css _site/public/

      - name: Verify staged production build
        run: |
          grep -q "Franchise Intelligence System" _site/index.html
          grep -q "Depth Charts" _site/index.html
          test -f _site/depth-chart.html
          grep -q "ALL 32 NFL TEAMS" _site/depth-chart.html
          test -f _site/public/depth-page.js
          test -f _site/public/depth-chart.css

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Verify deployed Pages build
        run: |
          url="${{ steps.deployment.outputs.page_url }}"
          echo "Checking $url"
          for i in 1 2 3 4 5 6; do
            home=$(curl --fail --silent --show-error "$url" || true)
            depth=$(curl --fail --silent --show-error "${url%/}/depth-chart.html" || true)
            if echo "$home" | grep -q "Depth Charts" && echo "$depth" | grep -q "ALL 32 NFL TEAMS"; then
              echo "GitHub Pages is serving the all-NFL depth chart tab."
              exit 0
            fi
            sleep 10
          done
          echo "GitHub Pages deployment is not serving the expected depth chart build."
          exit 1
