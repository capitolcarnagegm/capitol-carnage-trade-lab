{
  "name": "gms-locker-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run",
    "check": "wrangler deploy --dry-run",
    "db:migrate:local": "wrangler d1 migrations apply gms-locker-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply gms-locker-db --remote",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "4.120.0"
  }
}
